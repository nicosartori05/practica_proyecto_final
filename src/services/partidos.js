export async function cargarPartido ({fields /*Campos que se pidieron en el form*/}) {
    try {
        
    } catch (err) {
        console.log("[partidos.js cargarPartido()] Error al cargar el partido", err);
        throw err;
    }
}